// Stub — Plan 02-02 will replace this with the full implementation
import { HttpInterceptorFn } from '@angular/common/http';

export const errorInterceptor: HttpInterceptorFn = (req, next) => next(req);
